{
  "name": "assignment",
  "version": "1.0.0",
  "description": "Leave app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon index.js",
    "client": "cd client && npm start",
    "dev": "concurrently -n 'client,server'  \"npm run client\" \"npm run server\" "
  },
  "author": "anurag kumar",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^6.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.0",
    "nodemon": "^2.0.7"
  }
}
